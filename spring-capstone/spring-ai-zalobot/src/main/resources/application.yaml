spring:
  application:
    name: spring-ai-zalobot

  ai:
    google:
      genai:
        api-key: ${GEMINI_API_KEY}
        chat:
          options:
            model: gemini-3-flash-preview

zalobot:
  bot-token: ${ZALO_BOT_TOKEN}
  listener:
    poll-timeout: 30